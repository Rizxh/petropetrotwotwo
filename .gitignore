# Dependencies & build
node_modules/
dist/
dist-ssr/
*.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment & secrets
.env
.env.*
!.env.example

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
Thumbs.db

# Claude / local tooling
.claude/

# Local reference dumps (not for deploy)
web/File/


# Static HTML export — regenerate with: cd web && npm run export:html
/html-export/

# Source drops for the profile decks. Byte-identical copies already live in
# web/public/assets/, which is what the site actually serves.
/web/petrotwo-group/
/web/globalbiz/

# Aswangga site — built here for handoff, but lives in its own repo.
/aswangga/
